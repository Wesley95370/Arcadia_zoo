<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}Arcadia Zoo{% endblock %}</title>

    {# Google Fonts - chargées pour tout le site #}
    <link href="https://fonts.googleapis.com/css2?family=Caveat&family=Peralta&display=swap" rel="stylesheet">

    {# Styles généraux de base, pour tout le site (public) #}
    {{ encore_entry_link_tags('app') }}

    {# Styles spécifiques injectés par les pages (ex : admin) #}
    {% block extra_stylesheets %}{% endblock %}
</head>
<body class="{% block body_class %}{% endblock %}">

    {# Contenu principal injecté par chaque page #}
    {% block body %}{% endblock %}

    {# JS de base (si nécessaire), puis JS supplémentaires injectés via bloc #}
    {{ encore_entry_script_tags('app') }}
    {% block extra_javascripts %}{% endblock %}
</body>
</html>
